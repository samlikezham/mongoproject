<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<% include ../partials/head.ejs %>
	<title></title>
</head>
<body>
	<% include ../partials/nav.ejs %>

	<nav>
		<a href="/catalog">Back to Home</a>
	</nav>


	<div class="container">
		<h3><%= items.name %><br/></h3>

		<% if (currentAdmin) { %> 
           <span class="edit"><a href="/catalog/<%=items.id%>/edit" class="btn">Edit</a></span>
        	<span class="delete"><form action="/catalog/<%=items.id%>?_method=DELETE" method="POST">
				<input type="submit" value="DELETE"/>
				</form>
			</span>
		<% } %>
		<div class="row showImg">
			<div class="col-sm-6">
				<img src="<%=items.img%>"><br/><br/>
			</div>
			<div class="col-sm-6">
		<div class="itemDescription">
		<strong>Description:</strong>
		<%= items.description %><br/>
		<strong>Price:</strong>
		$<%= items.price %><br/>
		<strong>Quantity Remaining:</strong>
		<%= items.qty %>

		<% if (items.qty > 0){ %>
          <form action='/catalog/<%=items.id%>/buy?_method=PUT' method='POST'>
          	<span class="addToCart"><a href="/catalog/add-to-cart/<%=items.id%>" class="btn" role="button">Add To Cart</a></span>
          </form>
          <% } else { %>
            <button>Out of Stock</button>
        <% } %>
			
        </div>
	</div>
</div>

	
            
          
</body>
</html>